import { test, chromium } from '@playwright/test';

test.describe('Markopy UI Tests', () => {
  test.skip('Popup UI renders correctly', async () => {
    // Full UI testing requires extension service worker setup
    // This is a placeholder for future implementation
  });
});
